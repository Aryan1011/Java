package com.pkware.Threads;

public class ThreadConstructors {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		
		// Thread(), Thread(Runnable Target), Thread(String name), Thread(target,name)
		// Thread(TG, Target), (Tg,name), (Tg, target, name)
//		Thread(TG,T,name,long stackSize)
		
//		Methods
		/* Run, start,(static) threadRefence currentThread(), bool isAlive 
		 * getName, setname
		 * isDaemon, setDaemon(boolean)
		 * getPriority, setPriority(int), 
		 * sleep(time), yield(), join(), (Deprecated - suspend, resume, stop destroy)
		 * interrupt, isInterrupted, interrupted
		 * Not in thread class - wait notify notifyAll
		 * in the object class
		 */ 
		
		
	}

}
